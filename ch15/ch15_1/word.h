#ifndef WORD_H
#define WORD_H

// reads the next word and stores it in variable word
// truncates the word if length exceeds len
// makes word empty if word could not be read
void read_word(char* word, int len);

#endif